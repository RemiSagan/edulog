{% extends 'base.html.twig' %}

{% block title %}Specialty{% endblock %}

{% block body %}
    <h1>Specialty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialty.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ specialty.name }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ specialty.duration }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ specialty.createdAt ? specialty.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ specialty.updatedAt ? specialty.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('specialty_index') }}">back to list</a>

    <a href="{{ path('specialty_edit', {'id': specialty.id}) }}">edit</a>

    {{ include('specialty/_delete_form.html.twig') }}
{% endblock %}
